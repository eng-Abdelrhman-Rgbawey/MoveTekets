@using WebApplication1.Models
@model IEnumerable<Producer>
@{
    ViewData["Title"] = "Producers List";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2 class="fw-bold text-primary">🎬 Producers List</h2>
        <a asp-action="Create" class="btn btn-success">
            <i class="bi bi-person-plus"></i> Add New Producer
        </a>
    </div>

    <div class="table-responsive shadow rounded bg-white p-3">
        <table class="table table-hover align-middle text-center">
            <thead class="table-dark">
                <tr>
                    <th>Profile Picture</th>
                    <th>Full Name</th>
                    <th>Biography</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var producer in Model)
                {
                    <tr>
                        <td>
                            <img src="@producer.ProfilePicture" alt="@producer.FullName"
                                 class="rounded-circle shadow-sm border"
                                 style="width:100px; height:100px; object-fit:cover;" />
                        </td>

                        <td class="fw-semibold">
                            @producer.FullName
                        </td>

                        <td class="text-muted" style="max-width: 300px;">
                            @producer.Bio
                        </td>

                        <td>
                            <a asp-action="Edit" asp-route-id="@producer.id"
                               class="btn btn-sm btn-outline-primary me-1">
                                <i class="bi bi-pencil-square"></i> Edit
                            </a>

                            <a asp-action="Details" asp-route-id="@producer.id"
                               class="btn btn-sm btn-outline-info me-1">
                                <i class="bi bi-eye"></i> Details
                            </a>

                            <a asp-action="Delete" asp-route-id="@producer.id"
                               class="btn btn-sm btn-outline-danger">
                                <i class="bi bi-trash"></i> Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
